/**
 * MTemplate: Plantilla para copiar a la aplicacion 
 */
Ext.define('App.controller.MenuPrincipal', {
	extend:'M.controller.MenuPrincipal',

	views : ['MenuPrincipal'],

	init:function (app) {
	}

});
